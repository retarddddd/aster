{
  "schemaVersion": 1,
  "id": "aster",
  "version": "${version}",
  "name": "Aster",
  "environment": "client",
  "entrypoints": {
    "client": [
      {
        "adapter": "kotlin",
        "value": "cx.kloinn.aster.Main"
      }
    ]
  },
  "mixins": [
    "mixins.json"
  ],
  "depends": {
    "fabricloader": ">=${loader_version}",
    "fabric-language-kotlin": ">=${kotlin_loader_version}",
    "minecraft": "~${minecraft_version}",
    "fabric-api": "*"
  }
}
